1 - Create a poster component that takes an image, title,and text as props.
Render something like the sample image.

2 - Create a single line email, as would appear in an inbox. Reference the included screenshot. It should accept and email prop, which contains the sender, subject, date and message.